@model IEnumerable<Krd1.Models.BeerRecipe>

@{
    Layout = "~/Views/_Layout1.cshtml";
}

<h1>Piwne receptury</h1>
<ul class="list-group">
@foreach (var recipe in Model)
{
    <li class="list-group-item">@Html.ActionLink(@recipe.Name, "ShowRecipe", new {id = recipe.Id})</li>
}
</ul>